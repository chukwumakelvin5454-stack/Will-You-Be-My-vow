<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Will You Be My Vow?</title>
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', Tahoma, sans-serif;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      background: linear-gradient(135deg, #ff9a9e, #fad0c4);
      text-align: center;
    }

    .card {
      background: white;
      padding: 30px 20px;
      border-radius: 20px;
      width: 90%;
      max-width: 350px;
      box-shadow: 0 10px 25px rgba(0,0,0,0.2);
      position: relative;
    }

    h1 {
      font-size: 22px;
      margin-bottom: 20px;
      color: #333;
    }

    .buttons {
      margin-top: 20px;
      position: relative;
      height: 150px;
    }

    button {
      padding: 12px 20px;
      font-size: 16px;
      border: none;
      border-radius: 25px;
      cursor: pointer;
      position: absolute;
      transition: 0.2s;
    }

    #yesBtn {
      background: #28a745;
      color: white;
      left: 30px;
      top: 50px;
    }

    #noBtn {
      background: #dc3545;
      color: white;
      left: 180px;
      top: 50px;
    }

    .popup {
      display: none;
      position: fixed;
      inset: 0;
      background: rgba(0,0,0,0.6);
      align-items: center;
      justify-content: center;
      z-index: 10;
    }

    .popup-content {
      background: white;
      padding: 20px;
      border-radius: 15px;
      text-align: center;
      width: 80%;
      max-width: 300px;
    }

    .popup-content img {
      width: 100%;
      border-radius: 10px;
    }

    .popup-content p {
      margin-top: 10px;
      font-weight: bold;
      color: #333;
    }

    #success {
      display: none;
      font-size: 20px;
      color: #28a745;
      font-weight: bold;
    }
  </style>
</head>
<body>

  <div class="card">
    <h1>Will you be my vow? 💍❤️</h1>

    <div class="buttons">
      <button id="yesBtn">YES 😍</button>
      <button id="noBtn">NO 🙄</button>
    </div>

    <div id="success">
      Yayyy! I knew you would say YES! 🥰💖
    </div>
  </div>

  <div class="popup" id="popup">
    <div class="popup-content">
      <img src="https://media.giphy.com/media/3o6ZtaO9BZHcOjmErm/giphy.gif" alt="Funny" />
      <p>Stop messing with me 😭😂</p>
      <br/>
      <button onclick="closePopup()">Okay okay 😅</button>
    </div>
  </div>

  <script>
    const noBtn = document.getElementById("noBtn");
    const yesBtn = document.getElementById("yesBtn");
    const popup = document.getElementById("popup");
    const success = document.getElementById("success");

    let noCount = 0;

    function moveNoButton() {
      noCount++;

      const maxX = 250;
      const maxY = 120;

      const randomX = Math.random() * maxX;
      const randomY = Math.random() * maxY;

      noBtn.style.left = randomX + "px";
      noBtn.style.top = randomY + "px";

      if (noCount === 4) {
        popup.style.display = "flex";
      }
    }

    noBtn.addEventListener("click", moveNoButton);
    noBtn.addEventListener("touchstart", moveNoButton);

    yesBtn.addEventListener("click", () => {
      success.style.display = "block";
      noBtn.style.display = "none";
      yesBtn.style.display = "none";
    });

    function closePopup() {
      popup.style.display = "none";
      noCount = 0;
    }
  </script>

</body>
</html>